#!/usr/bin/node

/* Script that prints 3 lines by using an array of strings and a loop */

const Myarray = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; Myarray[i]; i++) {
  console.log(Myarray[i]);
}
